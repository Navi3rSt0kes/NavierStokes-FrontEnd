import { ArrowRight, Check, ChevronRight, Clock, CreditCard, Heart, MapPin, Package, Search, ShieldCheck, SlidersHorizontal, Sparkles, Star, Truck } from 'lucide-react'
import { Link, useLocation, useNavigate, useParams, useSearchParams } from 'react-router-dom'
import { useEffect, useState } from 'react'
import { categories, formatPrice, products, recipes } from './lib/data'
import ProductCard from './components/ProductCard'
import { useStore } from './store/useStore'
import { api } from './lib/api'

const SectionTitle = ({ eyebrow, title, link }) => <div className="mb-6 flex items-end justify-between gap-4"><div>{eyebrow && <p className="mb-2 text-xs font-bold uppercase tracking-[.18em] text-emerald-700">{eyebrow}</p>}<h2 className="text-2xl font-bold tracking-tight text-slate-900 sm:text-3xl">{title}</h2></div>{link && <Link to={link} className="flex items-center gap-1 text-sm font-semibold text-emerald-700 hover:text-emerald-800">Ver todo <ArrowRight size={16}/></Link>}</div>

export function HomePage() { const addToCart = useStore((state) => state.addToCart); return <div><section className="border-b border-stone-200 bg-[#f4f7ed]"><div className="mx-auto grid max-w-7xl gap-10 px-4 py-14 sm:px-6 lg:grid-cols-2 lg:px-8 lg:py-20"><div className="self-center"><span className="inline-flex items-center gap-2 rounded-full bg-white px-3 py-1.5 text-xs font-bold text-emerald-700"><Sparkles size={14}/> Entrega en menos de 60 min</span><h1 className="mt-5 max-w-xl text-4xl font-bold leading-[1.08] tracking-tight text-slate-900 sm:text-5xl">Todo lo que necesitas, <span className="text-emerald-600">en un solo lugar.</span></h1><p className="mt-5 max-w-lg text-lg leading-8 text-slate-600">Productos frescos, precios justos y una experiencia de compra que se adapta a tu día.</p><Link to="/shop" className="mt-7 inline-flex items-center gap-2 rounded-xl bg-emerald-600 px-5 py-3.5 font-semibold text-white transition hover:bg-emerald-700">Comprar ahora <ArrowRight size={18}/></Link></div><div className="relative mx-auto grid w-full max-w-lg grid-cols-3 gap-3 rounded-[2rem] bg-emerald-100 p-6"><span className="col-span-2 grid aspect-[1.35] place-items-center rounded-3xl bg-white text-8xl shadow-sm">🥬</span><span className="grid place-items-center rounded-3xl bg-amber-100 text-6xl">🥖</span><span className="grid place-items-center rounded-3xl bg-rose-100 text-6xl">🍓</span><span className="col-span-2 grid aspect-[1.55] place-items-center rounded-3xl bg-white text-7xl shadow-sm">🛒</span></div></div></section><div className="mx-auto max-w-7xl space-y-16 px-4 py-14 sm:px-6 lg:px-8"><section><SectionTitle eyebrow="Explora por sección" title="Todo para tu mercado" link="/categories"/><div className="grid grid-cols-2 gap-3 sm:grid-cols-4 lg:grid-cols-8">{categories.map((category) => <Link to={`/category/${category.id}`} key={category.id} className={`${category.color} group rounded-2xl p-4 transition hover:-translate-y-1`}><span className="text-3xl">{category.emoji}</span><p className="mt-5 text-sm font-bold text-slate-800">{category.name}</p><ChevronRight size={15} className="mt-1 text-slate-500 transition group-hover:translate-x-1"/></Link>)}</div></section><section className="rounded-3xl bg-slate-900 p-6 sm:p-10"><div className="grid gap-6 lg:grid-cols-[1fr_2fr]"><div><p className="text-xs font-bold uppercase tracking-[.18em] text-emerald-400">Ofertas de la semana</p><h2 className="mt-2 text-3xl font-bold text-white">Buenos precios para días mejores.</h2><Link to="/offers" className="mt-5 inline-block text-sm font-bold text-emerald-300">Ver ofertas →</Link></div><div className="grid grid-cols-2 gap-3 sm:grid-cols-4">{products.slice(0,4).map((product) => <div key={product.id} className="rounded-2xl bg-white p-3 text-slate-900"><div className="text-4xl">{product.emoji}</div><p className="mt-3 text-sm font-semibold">{product.name}</p><p className="mt-1 font-bold text-emerald-700">{formatPrice(product.price)}</p></div>)}</div></div></section><section><SectionTitle eyebrow="Favoritos de la comunidad" title="Más vendidos" link="/shop"/><div className="grid grid-cols-2 gap-4 sm:grid-cols-3 lg:grid-cols-5">{products.slice(0,5).map((product) => <ProductCard product={product} key={product.id}/>)}</div></section><section><SectionTitle eyebrow="Cocina sin complicarte" title="Recetas para hoy" link="/recipes"/><div className="grid gap-5 md:grid-cols-3">{recipes.map((recipe) => <article key={recipe.id} className="overflow-hidden rounded-2xl border border-stone-200 bg-white"><div className="grid aspect-[1.7] place-items-center bg-orange-100 text-7xl">{recipe.emoji}</div><div className="p-5"><div className="flex items-center gap-3 text-xs text-slate-500"><span className="flex items-center gap-1"><Clock size={14}/>{recipe.time}</span><span>{recipe.difficulty}</span></div><h3 className="mt-2 text-lg font-bold">{recipe.name}</h3><button onClick={() => recipe.ingredients.forEach((id) => addToCart(products.find((product) => product.id === id)))} className="mt-4 text-sm font-bold text-emerald-700">Comprar ingredientes →</button></div></article>)}</div></section><section className="grid gap-4 rounded-3xl border border-stone-200 bg-white p-6 sm:grid-cols-4"><Benefit icon={<Truck/>} title="Entrega rápida" text="Tu compra llega cuando la necesitas."/><Benefit icon={<Sparkles/>} title="Siempre fresco" text="Seleccionamos lo mejor para ti."/><Benefit icon={<ShieldCheck/>} title="Pago seguro" text="Compra con total tranquilidad."/><Benefit icon={<Heart/>} title="Estamos cerca" text="Soporte para cada momento."/></section></div></div> }
function Benefit({ icon, title, text }) { return <div className="flex gap-3"><span className="text-emerald-600">{icon}</span><div><h3 className="font-bold">{title}</h3><p className="mt-1 text-sm leading-5 text-slate-500">{text}</p></div></div> }

export function ShopPage({ categoryId }) { const [searchParams] = useSearchParams(); const query = searchParams.get('q')?.toLowerCase() || ''; const [sort, setSort] = useState('recommended'); const [active, setActive] = useState(categoryId || 'all'); let list = products.filter((item) => (active === 'all' || item.category === active) && (!query || item.name.toLowerCase().includes(query) || item.brand.toLowerCase().includes(query))); if (sort === 'low') list = [...list].sort((a,b) => a.price-b.price); if (sort === 'high') list = [...list].sort((a,b) => b.price-a.price); return <div className="mx-auto max-w-7xl px-4 py-10 sm:px-6 lg:px-8"><p className="text-sm font-medium text-emerald-700">Supermercado</p><h1 className="mt-2 text-3xl font-bold">Todo lo que buscas, más cerca.</h1><div className="mt-8 grid gap-8 lg:grid-cols-[240px_1fr]"><aside className="rounded-2xl border border-stone-200 bg-white p-5"><div className="flex items-center gap-2 font-bold"><SlidersHorizontal size={17}/> Filtros</div><p className="mt-5 text-xs font-bold uppercase tracking-wider text-slate-400">Categorías</p><div className="mt-2 space-y-1">{[['all','Todos'], ...categories.map((item) => [item.id,item.name])].map(([id,name]) => <button key={id} onClick={() => setActive(id)} className={`block w-full rounded-lg px-3 py-2 text-left text-sm ${active === id ? 'bg-emerald-50 font-semibold text-emerald-700' : 'text-slate-600 hover:bg-stone-50'}`}>{name}</button>)}</div><p className="mt-6 text-xs font-bold uppercase tracking-wider text-slate-400">Disponibilidad</p><label className="mt-3 flex gap-2 text-sm"><input type="checkbox" defaultChecked className="accent-emerald-600"/> En oferta</label><label className="mt-3 flex gap-2 text-sm"><input type="checkbox" className="accent-emerald-600"/> Mejor valorados</label></aside><section><div className="mb-5 flex items-center justify-between"><p className="text-sm text-slate-500">{list.length} productos encontrados</p><select value={sort} onChange={(event) => setSort(event.target.value)} className="rounded-lg border border-stone-200 bg-white px-3 py-2 text-sm outline-none"><option value="recommended">Recomendados</option><option value="low">Precio menor</option><option value="high">Precio mayor</option></select></div>{list.length ? <div className="grid grid-cols-2 gap-4 sm:grid-cols-3 xl:grid-cols-4">{list.map((product) => <ProductCard product={product} key={product.id}/>)}</div> : <Empty title="No encontramos exactamente lo que buscas." text="Prueba con otra búsqueda o explora nuestras categorías."/>}</section></div></div> }

export function CategoriesPage() { return <div className="mx-auto max-w-7xl px-4 py-10 sm:px-6 lg:px-8"><SectionTitle eyebrow="Encuentra tu sección" title="Compra por categorías"/><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{categories.map((category) => <Link key={category.id} to={`/category/${category.id}`} className={`${category.color} group rounded-3xl p-7 transition hover:-translate-y-1`}><span className="text-6xl">{category.emoji}</span><h2 className="mt-8 text-xl font-bold">{category.name}</h2><p className="mt-2 text-sm text-slate-600">{category.description}</p><span className="mt-5 inline-flex items-center gap-1 text-sm font-bold text-slate-700">Explorar <ArrowRight size={16}/></span></Link>)}</div></div> }

export function ProductPage() { const { id } = useParams(); const navigate = useNavigate(); const product = products.find((item) => item.id === id) || products[0]; const addToCart = useStore((state) => state.addToCart); const [quantity, setQuantity] = useState(1); const [tab, setTab] = useState('Descripción'); const add = () => Array.from({length: quantity}).forEach(() => addToCart(product)); return <div className="mx-auto max-w-7xl px-4 py-8 sm:px-6 lg:px-8"><nav className="mb-7 text-sm text-slate-500"><Link to="/shop">Supermercado</Link> <ChevronRight className="inline" size={14}/> {product.name}</nav><section className="grid gap-10 lg:grid-cols-2"><div className="grid aspect-square place-items-center rounded-3xl bg-stone-100 text-[10rem]">{product.emoji}</div><div><p className="text-sm font-semibold text-emerald-700">{product.brand}</p><h1 className="mt-2 text-3xl font-bold tracking-tight sm:text-4xl">{product.name}</h1><div className="mt-3 flex items-center gap-2 text-sm"><span className="flex items-center gap-1 text-amber-500"><Star size={16} fill="currentColor"/>{product.rating}</span><span className="text-slate-500">{product.reviews} reseñas</span></div><div className="mt-6 flex items-baseline gap-3"><span className="text-3xl font-bold">{formatPrice(product.price)}</span>{product.oldPrice && <span className="text-slate-400 line-through">{formatPrice(product.oldPrice)}</span>}</div><p className="mt-2 text-sm font-medium text-emerald-700">Disponible para entrega hoy</p><p className="mt-6 leading-7 text-slate-600">{product.description}</p><div className="mt-8 flex gap-3"><div className="flex items-center rounded-xl border border-stone-200"><button onClick={() => setQuantity(Math.max(1, quantity - 1))} className="px-4 py-3">−</button><span className="w-8 text-center font-bold">{quantity}</span><button onClick={() => setQuantity(quantity + 1)} className="px-4 py-3">+</button></div><button onClick={add} className="flex-1 rounded-xl bg-emerald-600 px-5 py-3 font-semibold text-white hover:bg-emerald-700">Agregar al carrito</button><button onClick={() => { add(); navigate('/checkout') }} className="rounded-xl border border-emerald-600 px-5 py-3 font-semibold text-emerald-700">Comprar ahora</button></div></div></section><section className="mt-14 border-t border-stone-200 pt-10"><div className="flex gap-6 border-b border-stone-200">{['Descripción','Información adicional'].map((item) => <button onClick={() => setTab(item)} key={item} className={`border-b-2 px-1 pb-3 text-sm font-semibold ${tab === item ? 'border-emerald-600 text-emerald-700' : 'border-transparent text-slate-500'}`}>{item}</button>)}</div><p className="max-w-2xl py-6 leading-7 text-slate-600">{tab === 'Descripción' ? product.description : `Presentación: ${product.unit}. Conserva el producto en un lugar fresco y seco.`}</p></section><section className="mt-8"><SectionTitle title="También te puede gustar"/><div className="grid grid-cols-2 gap-4 sm:grid-cols-4">{products.filter((item) => item.id !== product.id).slice(0,4).map((item) => <ProductCard key={item.id} product={item}/>)}</div></section></div> }

export function CartPage() { const { cart, updateQuantity, removeFromCart } = useStore(); const total = cart.reduce((sum,item) => sum+item.price*item.quantity,0); if (!cart.length) return <Empty title="Tu carrito está esperando" text="Agrega productos para comenzar tu compra." action="Ir al supermercado" href="/shop"/>; return <div className="mx-auto max-w-6xl px-4 py-10 sm:px-6"><h1 className="text-3xl font-bold">Tu carrito</h1><div className="mt-8 grid gap-8 lg:grid-cols-[1fr_340px]"><div className="space-y-3">{cart.map((item) => <div className="flex items-center gap-4 rounded-2xl border border-stone-200 bg-white p-4" key={item.id}><span className="grid h-18 w-18 place-items-center rounded-xl bg-stone-100 text-4xl">{item.emoji}</span><div className="min-w-0 flex-1"><h2 className="font-bold">{item.name}</h2><p className="text-sm text-slate-500">{item.unit}</p><p className="mt-1 font-bold">{formatPrice(item.price)}</p></div><div className="flex items-center rounded-lg border border-stone-200"><button onClick={() => updateQuantity(item.id,item.quantity-1)} className="px-3 py-2">−</button><span className="w-7 text-center">{item.quantity}</span><button onClick={() => updateQuantity(item.id,item.quantity+1)} className="px-3 py-2">+</button></div><button onClick={() => removeFromCart(item.id)} className="text-sm text-rose-600">Eliminar</button></div>)}</div><aside className="h-fit rounded-2xl border border-stone-200 bg-white p-6"><h2 className="text-lg font-bold">Resumen del pedido</h2><div className="mt-5 space-y-3 text-sm text-slate-600"><p className="flex justify-between"><span>Subtotal</span><span>{formatPrice(total)}</span></p><p className="flex justify-between"><span>Envío</span><span>{total >= 70000 ? 'Gratis' : formatPrice(7900)}</span></p></div><div className="mt-5 flex justify-between border-t border-stone-200 pt-5 text-lg font-bold"><span>Total</span><span>{formatPrice(total + (total >= 70000 ? 0 : 7900))}</span></div><Link to="/checkout" className="mt-6 block rounded-xl bg-emerald-600 py-3 text-center font-semibold text-white">Continuar compra</Link></aside></div></div> }

export function CheckoutPage() { const navigate = useNavigate(); const { cart, clearCart } = useStore(); const [delivery, setDelivery] = useState('Estándar · hoy de 4:00 a 6:00 p. m.'); const total = cart.reduce((sum,item) => sum+item.price*item.quantity,0); const submit = (event) => { event.preventDefault(); clearCart(); navigate('/order/success') }; if (!cart.length) return <Empty title="No hay productos para pagar" text="Agrega algunos productos antes de continuar." action="Ir al supermercado" href="/shop"/>; return <div className="mx-auto max-w-6xl px-4 py-10 sm:px-6"><p className="text-sm font-bold text-emerald-700">Checkout seguro</p><h1 className="mt-1 text-3xl font-bold">Finaliza tu pedido</h1><form onSubmit={submit} className="mt-8 grid gap-8 lg:grid-cols-[1fr_340px]"><div className="space-y-5"><CheckoutSection icon={<MapPin/>} title="1. Dirección de entrega"><div className="grid gap-3 sm:grid-cols-2"><input required defaultValue="Julia Rodríguez" aria-label="Nombre" className="input"/><input required defaultValue="300 000 0000" aria-label="Teléfono" className="input"/></div><input required defaultValue="Calle 72 # 10-20, Apartamento 402" aria-label="Dirección" className="input mt-3"/></CheckoutSection><CheckoutSection icon={<Truck/>} title="2. Método de entrega">{['Estándar · hoy de 4:00 a 6:00 p. m.','Rápida · en 60 minutos','Recoger en tienda'].map((option) => <label className={`mt-3 flex cursor-pointer items-center justify-between rounded-xl border p-3 text-sm ${delivery === option ? 'border-emerald-500 bg-emerald-50' : 'border-stone-200'}`} key={option}><span><input type="radio" checked={delivery === option} onChange={() => setDelivery(option)} className="mr-2 accent-emerald-600"/>{option}</span>{option.includes('Rápida') && <span className="font-semibold">+ {formatPrice(3500)}</span>}</label>)}</CheckoutSection><CheckoutSection icon={<CreditCard/>} title="3. Método de pago"><label className="flex items-center gap-2 rounded-xl border border-emerald-500 bg-emerald-50 p-3 text-sm"><input type="radio" defaultChecked className="accent-emerald-600"/> Tarjeta terminada en 4242</label><label className="mt-3 flex items-center gap-2 rounded-xl border border-stone-200 p-3 text-sm"><input type="radio" className="accent-emerald-600"/> Efectivo contra entrega</label></CheckoutSection></div><aside className="h-fit rounded-2xl border border-stone-200 bg-white p-6"><h2 className="font-bold">Resumen</h2><div className="mt-4 space-y-2 text-sm">{cart.map((item) => <p key={item.id} className="flex justify-between text-slate-600"><span>{item.quantity}× {item.name}</span><span>{formatPrice(item.quantity * item.price)}</span></p>)}</div><div className="mt-5 flex justify-between border-t border-stone-200 pt-5 font-bold"><span>Total</span><span>{formatPrice(total)}</span></div><button className="mt-6 w-full rounded-xl bg-emerald-600 py-3 font-semibold text-white">Confirmar pedido</button><p className="mt-3 text-center text-xs text-slate-500">Pago protegido y cifrado.</p></aside></form></div> }
function CheckoutSection({ icon, title, children }) { return <section className="rounded-2xl border border-stone-200 bg-white p-5"><h2 className="flex items-center gap-2 font-bold"><span className="text-emerald-600">{icon}</span>{title}</h2><div className="mt-4">{children}</div></section> }

export function SuccessPage() { return <div className="mx-auto max-w-xl px-4 py-20 text-center"><div className="mx-auto grid h-18 w-18 place-items-center rounded-full bg-emerald-100 text-emerald-600"><Check size={38}/></div><p className="mt-6 text-sm font-bold text-emerald-700">PEDIDO #MV-48291</p><h1 className="mt-2 text-3xl font-bold">¡Pedido confirmado!</h1><p className="mt-4 leading-7 text-slate-600">Tu pedido está confirmado y llegará hoy entre las 4:00 y 6:00 p. m. Te avisaremos cada paso del camino.</p><div className="mt-8 rounded-2xl border border-stone-200 bg-white p-5 text-left"><p className="text-sm text-slate-500">Dirección de entrega</p><p className="mt-1 font-semibold">Calle 72 # 10-20, Apartamento 402</p></div><div className="mt-7 flex justify-center gap-3"><Link to="/orders/MV-48291" className="rounded-xl bg-emerald-600 px-5 py-3 font-semibold text-white">Ver pedido</Link><Link to="/shop" className="rounded-xl border border-stone-200 px-5 py-3 font-semibold">Seguir comprando</Link></div></div> }

export function OrdersPage() { const { id } = useParams(); const detail = id; return <div className="mx-auto max-w-5xl px-4 py-10 sm:px-6"><h1 className="text-3xl font-bold">{detail ? `Pedido #${detail}` : 'Mis pedidos'}</h1>{detail ? <OrderDetail/> : <div className="mt-7 space-y-4"><Link to="/orders/MV-48291" className="block rounded-2xl border border-stone-200 bg-white p-5 hover:border-emerald-300"><div className="flex items-center justify-between"><div><p className="font-bold">Pedido #MV-48291</p><p className="mt-1 text-sm text-slate-500">Hoy · 5 productos</p></div><span className="rounded-full bg-amber-100 px-3 py-1 text-xs font-bold text-amber-700">Preparando</span></div><p className="mt-4 font-semibold">{formatPrice(82400)}</p></Link></div>}</div> }
function OrderDetail() { const stages = ['Pedido realizado','Confirmado','Preparando','En camino','Entregado']; return <div className="mt-8 grid gap-8 lg:grid-cols-[1fr_300px]"><section className="rounded-2xl border border-stone-200 bg-white p-6"><h2 className="font-bold">Estado de tu pedido</h2><ol className="mt-6 space-y-6">{stages.map((stage,index) => <li key={stage} className="flex gap-4"><span className={`grid h-7 w-7 shrink-0 place-items-center rounded-full text-xs font-bold ${index < 3 ? 'bg-emerald-600 text-white' : 'bg-stone-100 text-slate-400'}`}>{index < 3 ? '✓' : index+1}</span><div><p className={index < 3 ? 'font-semibold' : 'text-slate-500'}>{stage}</p>{index === 2 && <p className="mt-1 text-sm text-slate-500">Estamos preparando tus productos.</p>}</div></li>)}</ol></section><aside className="rounded-2xl bg-[#f4f7ed] p-6"><Package className="text-emerald-700"/><h2 className="mt-4 font-bold">Entrega estimada</h2><p className="mt-1 text-sm text-slate-600">Hoy, 4:00 - 6:00 p. m.</p></aside></div> }

export function RecipesPage() { const addToCart = useStore((state) => state.addToCart); return <div className="mx-auto max-w-7xl px-4 py-10 sm:px-6"><SectionTitle eyebrow="Ideas que inspiran" title="Recetas para disfrutar"/><div className="grid gap-5 md:grid-cols-3">{recipes.map((recipe) => <article key={recipe.id} className="rounded-2xl border border-stone-200 bg-white p-5"><div className="grid aspect-[1.6] place-items-center rounded-xl bg-orange-100 text-7xl">{recipe.emoji}</div><div className="mt-5 flex items-center gap-3 text-xs text-slate-500"><span>{recipe.time}</span><span>·</span><span>{recipe.difficulty}</span></div><h2 className="mt-2 text-xl font-bold">{recipe.name}</h2><p className="mt-2 text-sm text-slate-500">Ingredientes seleccionados, una receta clara y mucho sabor.</p><button onClick={() => recipe.ingredients.forEach((id) => addToCart(products.find((product) => product.id === id)))} className="mt-5 rounded-xl bg-emerald-600 px-4 py-2.5 text-sm font-semibold text-white">Comprar ingredientes</button></article>)}</div></div> }

export function OffersPage() { return <div className="mx-auto max-w-7xl px-4 py-10 sm:px-6"><section className="rounded-3xl bg-rose-600 px-7 py-12 text-white sm:px-12"><p className="text-sm font-bold uppercase tracking-widest text-rose-100">Ofertas de temporada</p><h1 className="mt-3 max-w-xl text-4xl font-bold">Ahorra más en lo que compras siempre.</h1><p className="mt-4 max-w-lg text-rose-50">Promociones seleccionadas para llenar tu despensa y cuidar tu bolsillo.</p></section><div className="mt-10"><SectionTitle title="Precios especiales"/><div className="grid grid-cols-2 gap-4 sm:grid-cols-3 lg:grid-cols-5">{products.filter((product) => product.oldPrice || product.badge).map((product) => <ProductCard key={product.id} product={product}/>)}</div></div></div> }

export function WishlistPage() { const { favorites, addToCart } = useStore(); return <div className="mx-auto max-w-7xl px-4 py-10 sm:px-6"><SectionTitle title="Tus favoritos"/>{favorites.length ? <div className="grid grid-cols-2 gap-4 sm:grid-cols-3 lg:grid-cols-5">{favorites.map((product) => <ProductCard product={product} key={product.id}/>)}</div> : <Empty title="Aún no tienes favoritos" text="Guarda los productos que más te gustan para encontrarlos rápidamente." action="Explorar productos" href="/shop"/>}</div> }

export function ListsPage() { const addToCart = useStore((state) => state.addToCart); const [lists, setLists] = useState([{name:'Mercado semanal', items:['leche','arroz','tomate'], checked: false},{name:'Desayunos', items:['pan','yogurt','cafe'], checked:false}]); return <div className="mx-auto max-w-5xl px-4 py-10 sm:px-6"><div className="flex items-center justify-between"><div><p className="text-sm font-bold text-emerald-700">Organiza tu compra</p><h1 className="mt-1 text-3xl font-bold">Mis listas</h1></div><button onClick={() => setLists([...lists,{name:'Nueva lista',items:[],checked:false}])} className="rounded-xl bg-emerald-600 px-4 py-2.5 text-sm font-semibold text-white">Crear lista</button></div><div className="mt-8 grid gap-4 md:grid-cols-2">{lists.map((list,index) => <article className="rounded-2xl border border-stone-200 bg-white p-5" key={index}><h2 className="font-bold">{list.name}</h2><p className="mt-1 text-sm text-slate-500">{list.items.length} productos</p><div className="mt-4 space-y-2">{list.items.map((id) => <label className="flex items-center gap-2 text-sm" key={id}><input type="checkbox" className="accent-emerald-600"/> {products.find((product) => product.id === id)?.name}</label>)}</div><button onClick={() => list.items.forEach((id) => addToCart(products.find((product) => product.id === id)))} className="mt-5 text-sm font-bold text-emerald-700">Agregar todo al carrito →</button></article>)}</div></div> }

export function AccountPage() { const sections = ['Perfil','Direcciones','Métodos de pago','Preferencias','Notificaciones']; return <div className="mx-auto max-w-5xl px-4 py-10 sm:px-6"><h1 className="text-3xl font-bold">Mi cuenta</h1><div className="mt-7 grid gap-5 md:grid-cols-[220px_1fr]"><aside className="rounded-2xl border border-stone-200 bg-white p-3">{sections.map((section,index) => <button className={`block w-full rounded-lg px-3 py-2.5 text-left text-sm ${index===0 ? 'bg-emerald-50 font-semibold text-emerald-700' : 'text-slate-600'}`} key={section}>{section}</button>)}</aside><section className="rounded-2xl border border-stone-200 bg-white p-6"><h2 className="text-lg font-bold">Perfil</h2><div className="mt-5 grid gap-3 sm:grid-cols-2"><input defaultValue="Julia Rodríguez" className="input"/><input defaultValue="julia@email.com" className="input"/></div><button className="mt-5 rounded-xl bg-slate-900 px-4 py-2.5 text-sm font-semibold text-white">Guardar cambios</button></section></div></div> }

export function Empty({ title, text, action, href }) { return <div className="mx-auto max-w-md px-4 py-20 text-center"><div className="text-5xl">🛒</div><h1 className="mt-5 text-2xl font-bold">{title}</h1><p className="mt-3 leading-6 text-slate-500">{text}</p>{action && <Link to={href} className="mt-6 inline-block rounded-xl bg-emerald-600 px-5 py-3 font-semibold text-white">{action}</Link>}</div> }
